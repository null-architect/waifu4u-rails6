<%= form_with model: @waifu, local: true do |form| %>

  <% if @waifu.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@waifu.errors.count, "error") %> prohibited
        this waifu from being saved:
      </h2>
      <ul>
        <% @waifu.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>

  <p>
    <%= form.label :anime %><br>
    <%= form.text_field :anime %>
  </p>

  <p>
    <%= form.file_field :image %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>